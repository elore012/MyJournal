<!DOCTYPE html>
<html>
  <head>
    <title>Journalv</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div id="devise-main">
  <div>
  <% if user_signed_in? %>
  <h1> Welcome <%=current_user.email%></h1>
  <button class="button"><%= link_to 'Signout',destroy_user_session_path,method: :delete,data:{confirm:"ok"}%></button>
  <%else%>
  <button class ="button"><%= link_to 'Sign in', new_user_session_path %></button>
  <button class ="button"><%= link_to 'Sign up', new_user_registration_path %></button>
  <%end%>
  </div>
  </div>
    <%= yield %>
  </body>
</html>
